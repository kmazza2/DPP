name: Ubuntu x64
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-24.04
    steps:
      - run: cd ${{ github.workspace }} && configure && make
      - run: ${{ github.workspace }}/test_ieee
